# TravelMantics
An app that uses firebase realtime database to authenticate users,read and write the travel deals to the database.
It also utilizes the firebase storage to upload data i.e images.I have authenticated the users using the FirebaseUI
I have also used recycler view to display the list of the available places.
